class Permissions {
  // ── Dashboard ──────────────────────────────────────────────────────────────
  static const dashboard = 'ViewDashboard';

  // ── Cases ──────────────────────────────────────────────────────────────────
  static const viewCases = 'ViewCases';
  static const createCases = 'CreateCases';
  static const editCases = 'EditCases';
  static const deleteCases = 'DeleteCases';

  // ── Courts ─────────────────────────────────────────────────────────────────
  static const viewCourts = 'ViewCourts';

  // ── Customers ──────────────────────────────────────────────────────────────
  static const viewCustomers = 'ViewCustomers';

  // ── Hearings ───────────────────────────────────────────────────────────────
  static const viewHearings = 'ViewHearings';

  // ── Notifications ──────────────────────────────────────────────────────────
  static const viewNotifications = 'ViewNotifications';

  // ── Billing ────────────────────────────────────────────────────────────────
  static const viewBilling = 'ViewBilling';
  static const createBilling = 'CreateBilling';
  static const deleteBilling = 'DeleteBilling';

  // ── Trust Accounting ───────────────────────────────────────────────────────
  static const viewTrustAccounting = 'ViewTrustAccounting';
  static const createTrustAccounting = 'CreateTrustAccounting';
  static const editTrustAccounting = 'EditTrustAccounting';
  static const deleteTrustAccounting = 'DeleteTrustAccounting';

  // ── Client Portal ──────────────────────────────────────────────────────────
  static const viewClientPortal = 'ViewClientPortal';
  static const manageClientPortal = 'ManageClientPortal';

  // ── Settings ───────────────────────────────────────────────────────────────
  static const manageSettings = 'ManageSettings';

  // ── Employees ──────────────────────────────────────────────────────────────
  static const viewEmployees = 'ViewEmployees';
  static const createEmployees = 'CreateEmployees';
  static const editEmployees = 'EditEmployees';

  // ── Contenders & Governments ───────────────────────────────────────────────
  static const viewContenders = 'ViewContenders';
  static const viewGovernments = 'ViewGovernments';

  // ── Judicial Documents ─────────────────────────────────────────────────────
  static const viewJudicial = 'ViewJudicial';

  // ── Documents & Files ──────────────────────────────────────────────────────
  static const viewDocuments = 'ViewDocuments';
  static const viewFiles = 'ViewFiles';

  // ── Tasks ──────────────────────────────────────────────────────────────────
  static const viewTasks = 'ViewTasks';

  // ── Time Tracking ──────────────────────────────────────────────────────────
  static const viewTimeTracking = 'ViewTimeTracking';

  // ── Calendar ───────────────────────────────────────────────────────────────
  static const viewCalendar = 'ViewCalendar';

  // ── Sitings ────────────────────────────────────────────────────────────────
  static const viewSitings = 'ViewSitings';

  // ── Consultations ──────────────────────────────────────────────────────────
  static const viewConsultations = 'ViewConsultations';

  // ── Reports ────────────────────────────────────────────────────────────────
  static const viewReports = 'ViewReports';
  static const viewTrustReports = 'ViewTrustReports';

  // ── Intake / Leads ─────────────────────────────────────────────────────────
  static const viewIntake = 'ViewIntake';
  static const manageIntake = 'ManageIntake';
  static const qualifyLeads = 'QualifyLeads';

  // ── eSign ──────────────────────────────────────────────────────────────────
  static const viewESign = 'ViewESign';

  // ── AI & Automation ────────────────────────────────────────────────────────
  static const viewAiAssistant = 'ViewAiAssistant';
  static const viewDocumentGeneration = 'ViewDocumentGeneration';
  static const viewCourtAutomation = 'ViewCourtAutomation';

  // ── Work Queue ─────────────────────────────────────────────────────────────
  static const viewWorkqueue = 'ViewWorkqueue';

  // ── Admin / SuperAdmin only ────────────────────────────────────────────────
  static const manageUsers = 'ManageUsers';
  static const manageTenants = 'ManageTenants';
  static const viewAuditLogs = 'ViewAuditLogs';
  static const manageAdministration = 'ManageAdministration';
  static const manageSubscription = 'ManageSubscription';
}
