# Copy this file to .env and update values for your local environment
# Do NOT commit your .env with secrets

# Base API URL (include /api) - used by the client to call the backend
VITE_API_BASE_URL=http://localhost:5000/api

# Optional: Application display name
VITE_APP_NAME=LawyerSys Client
